{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "namespaceclass-controller.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "namespaceclass-controller.resourceName" (dict "suffix" "namespaceclass-admin-role" "context" $) }}
rules:
- apiGroups:
  - namespaceclass.akuity.io
  resources:
  - namespaceclasses
  verbs:
  - '*'
- apiGroups:
  - namespaceclass.akuity.io
  resources:
  - namespaceclasses/status
  verbs:
  - get
{{- end }}
